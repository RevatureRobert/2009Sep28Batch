import React from 'react';

export const FunctionClicker: React.FC<any> = () => { // This IS a render function
    return (
        <div>
            <button>Button from a function!</button>
        </div>
    );
};// it'll be mostly functional for the coursework ...
// functions used to be unable to manage any data that wasn't passed in here,
// but this changed in a later update ...

